def max_num(arr):
    max = arr[0]
    for el in arr:
        if el > max:
            max = el
    return max


def min_num(arr):
    min =arr[0]
    for el in arr:
        if el < min:
            min = el
    return min

